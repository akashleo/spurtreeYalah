import * as React from "react";
import { Appbar } from "react-native-paper";

const Header = () => (
  <Appbar.Header>
    <Appbar.Content title="Yalah" />
    <Appbar.Action icon="infinity" onPress={() => {}} />
  </Appbar.Header>
);

export default Header;
